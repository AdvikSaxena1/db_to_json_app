# db_to_json_app
A database metadata extraction tool that converts SQLite (.db) files into structured JSON format. Upload a database file, automatically extract tables, columns, relationships, and schema details, and download the generated JSON metadata file through a simple web interface.
